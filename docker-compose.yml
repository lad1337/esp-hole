services:
  blocklist-generator:
    build: ./generator
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./generator/config.json:/config.json:ro
      - blocklist-data:/data

volumes:
  blocklist-data:
