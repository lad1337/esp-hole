services:
  blocklist-generator:
    build: ./generator
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8094:8094/udp"
    volumes:
      - ./generator/config.json:/config.json:ro
      - blocklist-data:/data

volumes:
  blocklist-data:
