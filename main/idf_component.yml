# IDF v6 moved cJSON and the PHY drivers out of the core tree into the
# component manager. ESP32-P4-only project: the Function-EV-Board's onboard
# IP101 (ip101 driver).
dependencies:
  espressif/cjson: "^1.7"
  espressif/ip101: "^1"
